#!/bin/bash
set -e
set -o pipefail

cd /app/src
poetry run python lm_zoo_util/get_surprisals.py \
  --vocab "$(< /app/model/vocab-file.txt)" \
  --dataset "$(< /app/model/dataset.txt)" \
  --input /app/model \
  $(< /app/model/extra-args.txt) \
  "$@"
